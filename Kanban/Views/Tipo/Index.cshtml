@model List<Data.Object.Tipo>

@{
    ViewBag.Title = "Index";

    <script>
        
    </script>    
}

@{
<h2>Tipo</h2>

    @Html.ActionLink("Novo", "Criar", null, htmlAttributes: new { @class="btn btn-primary"});
    @Html.ActionLink("Voltar", "Index", "Home", null, htmlAttributes: new { @class = "btn btn-default" });
    <br />
    <table class="table table-striped table-responsive">
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.idTipo</td>
                <td>@item.descricao</td>
                <td>
                    @Html.ActionLink("Editar", "Editar", new { statusId = item.idTipo }, htmlAttributes: new { @class = "btn btn-primary"})
                    @Html.ActionLink("Excluir", "Excluir", new { statusId = item.idTipo }, htmlAttributes: new { @class = "btn btn-danger" })
                </td>
            </tr>
        }
    </table>
}

